import { StyleSheet } from "react-native";
import { textColor } from "../helpers/stylePokemon";

export default StyleSheet.create({
  title: {
    fontSize: 26,
    color: textColor.white,
  },
  number: {
    fontSize: 12,
    color: textColor.number,
  },
});
